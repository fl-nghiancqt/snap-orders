package com.example.snaporder.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * SnapOrders Color Palette
 * 
 * Design Philosophy: Clean, modern, minimal white-green theme
 * Similar to premium restaurant / POS apps
 */
object SnapOrdersColors {

    // Main action color
    val Primary = Color(0xFF6F8F3D) // recommended
    val OnPrimary = Color(0xFFFFFFFF)

    // Softer green for backgrounds / wave
    val PrimaryContainer = Color(0xFF8FAE5D)
    val OnPrimaryContainer = Color(0xFF1E1E1E)

    // Background
    val Background = Color(0xFFFFFFFF)
    val Surface = Color(0xFFF7F8F5)

    // Text
    val TextPrimary = Color(0xFF1E1E1E)
    val TextSecondary = Color(0xFF7A7A7A)

    // Outline & error
    val Outline = Color(0xFFE5E7E2)
    val Error = Color(0xFFD32F2F)
}

